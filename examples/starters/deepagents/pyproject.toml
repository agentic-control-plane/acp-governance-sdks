[project]
name = "acp-starter-deepagents"
version = "0.0.1"
description = "Minimal ACP-governed Deep Agents starter: main agent and subagents under policy."
requires-python = ">=3.11"
dependencies = [
    "acp-langchain",
    "deepagents>=0.7",
    "langchain>=1.3.3",
    "langchain-openai>=0.3.0",
    "python-dotenv>=1.0.0",
]

[tool.uv.sources]
acp-langchain = { path = "../../../python/acp-langchain", editable = true }
